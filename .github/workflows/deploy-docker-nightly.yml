name: Docker Image Nightly Release

# Legacy OpenEthereum nightly Docker pipeline. Disabled — use release.yml.
on:
  workflow_dispatch:

jobs:
  deploy-docker:
    name: Build Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout sources
        uses: actions/checkout@v4

      - name: Install toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Build
        run: cargo build --release --bin ovbc
